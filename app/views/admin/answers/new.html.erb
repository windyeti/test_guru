<h1><%= t('.header') %></h1>
<h2><%= t('.for_question', question: @question.body) %></h2>
<%= render partial: 'shared/errors', locals: { resources: @answer } %>
<%= render 'form' %>
<%= link_to t('.back'), admin_question_path(@question) %>
