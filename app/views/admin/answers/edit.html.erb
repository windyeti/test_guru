<h1>Editing Answer</h1>
<h2>Для вопроса <%= @answer.question.body %></h2>

<%= render 'form' %>

<%= link_to 'Show', admin_answer_path(@answer) %> |
<%= link_to 'Back', admin_question_path(@answer.question) %>
