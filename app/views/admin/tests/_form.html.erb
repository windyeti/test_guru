<%= form_with(model: [:admin, @test], local: true) do |f| %>
  <p>
    <%= f.label(:title) %>
    <%= f.text_field(:title) %>
  </p>
  <p>
    <%= f.label(:level) %>
    <%= f.number_field(:level) %>
  </p>
  <p>
    <%= f.label(:category_id) %>
    <%= f.collection_select(:category_id, Category.all, :id, :title, prompt: t('helpers.select.admin.test.hint')) %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
