<nav class="navbar navbar-expand-lg navbar-light bg-light <%= 'admin_panel' if current_user.present? && current_user.admin? %>">
  <%= link_to 'Хочу все знать!', root_path, class: 'navbar-brand' %>
  <% if current_user.present? && current_user.admin? %>
    <div class=" navbar-text mr-3" >
      <%=  link_to t('.admin_panel'), admin_tests_path, class: 'btn btn-outline-light' %>
    </div>
    <div class=" navbar-text mr-3" >
       |
    </div>
    <div class=" navbar-text mr-3" >
      <%=  link_to t('.badge_panel'), new_admin_badge_path, class: 'btn btn-primary' %>
    </div>
  <% end %>
  <% if current_user.present?%>
    <div class=" navbar-text mr-3" >
      <%=  link_to t('.own_badges'), show_user_badge_path, class: 'btn btn-info' %>
    </div>
    <div class=" navbar-text mr-3" >
      <%=  link_to t('.sample_badges'), show_all_badge_path, class: 'btn btn-warning' %>
    </div>
  <% end %>
  <div class=" navbar-text" >
    <%= render partial: 'shared/menu' %>
  </div>
</nav>
